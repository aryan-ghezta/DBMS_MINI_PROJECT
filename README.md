# DBMS_MINI_PROJECT
1) In the folder named frontend and backend there is a app.py file which is written in flask and handles all backend logic.
2) Inside templates folder there are various frontend templates
3) Inside the file named pet_store_and_grooming_services.sql there is code to recreate entire database
4) inside procedure_trigger_function.sql there is code for all functions, procedures and triggers
